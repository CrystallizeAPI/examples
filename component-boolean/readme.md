# Component Boolean

Create an item with a boolean component

- Creates a new shape, `example-boolean`
- Creates a new item based on the shape, "Example item with boolean"

## Usage

1. Install dependencies
2. `yarn start` or `npm run start`
